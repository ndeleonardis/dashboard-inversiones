# === Datos reales / sensibles — NUNCA committear ===
data/real/

# El Excel real, esté donde esté
*.xlsx
# excepción: los de ejemplo (falsos) sí se guardan
!data/sample/*.xlsx
# temporales/lock de Excel (archivos "~$...") nunca se guardan
~$*.xlsx

# === Secretos / entorno ===
.env
.env.*

# === Dependencias ===
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# === Build / salida ===
dist/
build/
*.tsbuildinfo

# === Sistema operativo ===
.DS_Store
Thumbs.db

# === Editores ===
.idea/
